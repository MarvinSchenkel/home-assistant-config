sequence:
  - condition: and
    conditions:
      - condition: template
        value_template: "{{ is_state('input_select.marvin_sleep_status_dropdown', 'Slaapt') }}"
      - condition: template
        value_template: "{{ is_state('input_select.laura_sleep_status_dropdown', 'Slaapt') }}"
      - condition: state
        entity_id: light.bedside_right
        state: 'on'  
  - service: light.turn_off
    entity_id: light.bedside_right