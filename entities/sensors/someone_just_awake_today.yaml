---
platform: history_stats
name: someone_just_awake_today
entity_id: binary_sensor.house_mode_someone_just_awake
state: "on"
type: time
start: "{{ now().replace(hour=0, minute=0, second=0) }}"
end: "{{ now() }}"
