---
# Determine the presence in the bathroom based on
# - PIR motion sensor
# - Flood sensor
platform: template
sensors:
  presence_bathroom:
    value_template: >
      {{  is_state("binary_sensor.pir_bathroom", "on")
          or is_state("binary_sensor.notification_sensor_water_water_leak", "on") }}