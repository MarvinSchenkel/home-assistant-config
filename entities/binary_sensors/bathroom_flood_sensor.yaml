---
# Convert the useless integers from the flood sensor to on/off
platform: template
sensors:
  bathroom_flood:
    value_template: >
      {{  states("binary_sensor.notification_sensor_water_water_leak") }}